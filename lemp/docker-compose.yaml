version: "3.3"
services:
  nginx-proxy:
    image: jwilder/nginx-proxy
    container_name: nginx_proxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - /home/clau/certs:/etc/nginx/certs:ro
      - /etc/nginx/vhost.d
      - /usr/share/nginx/html
    labels:
      - com.github.jrcs.letsencrypt_nginx_proxy_companion.nginx_proxy
      
  letsencrypt:
    image: jrcs/letsencrypt-nginx-proxy-companion
    container_name: letsencrypt
    restart: always
    volumes:
      - /home/clau/certs:/etc/nginx/certs:rw
      - /var/run/docker.sock:/var/run/docker.sock:ro
    volumes_from:
      - nginx-proxy:rw

  web:
    image: claclin73/ngnix:1.25.1
    container_name: web2
    restart: always
    expose:
      - "80"
    volumes:
      - /home/clau/MANUALES/:/var/www/html/
      - /home/clau/MANUALES/default.conf:/etc/nginx/conf.d/default.conf
    links:
      - php-fpm
    environment:
      - VIRTUAL_HOST=botclaudi.ddns.net
      - LETSENCRYPT_HOST=botclaudi.ddns.net
      - LETSENCRYPT_EMAIL=claclin73@gmail.com

  web3:
    image: claclin73/ngnix:1.25.1
    container_name: web3
    restart: always
    expose:
      - "80"
    volumes:
      - /home/clau/MANUALES2/:/var/www/html/
      - /home/clau/MANUALES2/default.conf:/etc/nginx/conf.d/default.conf
    links:
      - php-fpm
    environment:
      - VIRTUAL_HOST=conocimientos.ddns.net
      - LETSENCRYPT_HOST=conocimientos.ddns.net
      - LETSENCRYPT_EMAIL=oscarramiro73@gmail.com

  # Configuración de php-fpm
  php-fpm:
    image: php:8-fpm
    container_name: php
    restart: always
    volumes:
      - /home/clau/MANUALES:/var/www/html

  mysql:
    image: mariadb
    container_name: mariadb
    restart: always
    ports: 
        - "3306:3306"
    environment:
        MYSQL_ROOT_PASSWORD: claudi 
    volumes:
        - /home/clau/mariadb/persistent:/var/lib/mysql
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: admin
    restart: always
    links: 
        - mysql:mysql
    ports:
        - "3309:80"
    environment:
        MYSQL_USERNAME: root
        MYSQL_ROOT_PASSWORD: claudi
        PMA_HOST: mysql
